import 'package:flutter/material.dart';

class BarberAgendaPage extends StatelessWidget {
  const BarberAgendaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('BarberAgendaPage')),
      body: const Center(
        child: Text('BarberAgendaPage - En desarrollo'),
      ),
    );
  }
}
